# ── Vite 빌드 환경변수 (예시) ──
# 실제 값은 .env 에 두세요(.env 는 .gitignore 대상). 이 파일은 커밋됩니다.
# Vite 규칙상 클라이언트에 노출되는 변수는 반드시 VITE_ 접두사가 필요합니다.

# Supabase Edge Function 프록시 베이스 URL 오버라이드 (미설정 시 src/config.js 기본값 사용)
# VITE_PROXY_BASE=https://<your-project-ref>.supabase.co/functions/v1/proxy
